import type { ProjectContent } from "../../types";

export default {
  "title": "Trabajo · Process Automation Pipeline",
  "theme": "dark",
  "tags": [
    "typescript",
    "playwright",
    "node"
  ],
  "description": "End-to-end pipeline with approval gates automating job search, CV adaptation, and offer application.<br/>Reflects exactly the scripting and automation mindset required in helpdesk: eliminating repetitive manual steps, chaining external tools, and ensuring auditable checkpoints — directly applicable to onboarding/offboarding workflows.",
  "components": []
} as const satisfies ProjectContent;
