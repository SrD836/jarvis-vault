---
title: Code Audit 2026-05-29
type: code-audit
date: 2026-05-29
source: cron/overnight-code-parallel
clusters: [observability, llm-routing, memory, vault-maintenance, autonomous-loops]
model: deepseek-chat
related:
  - "[[agents/auditor]]"
  - "[[agents/debugger]]"
  - "[[agents/tester]]"
tags: [code, audit, overnight, parallel]
---

# Code Audit — 2026-05-29

> Auditoria PARALELA autonoma (`hermes/overnight_audit.py`, DeepSeek). FASE 5. Clusters auditados: observability, llm-routing, memory, vault-maintenance, autonomous-loops. Fallidos: ninguno.

## Resumen ejecutivo
Se auditaron 5 clusters (observability, llm-routing, memory, vault-maintenance, autonomous-loops) con 17 hallazgos totales. Se identificaron 5 hallazgos de severidad ALTA, 7 MEDIA y 5 BAJA. No se encontraron hallazgos recurrentes de auditorías previas.

## Hallazgos por severidad
**ALTA**
- `run_logger.py`: `str(e).encode()` falla con caracteres no-ASCII en errores de subprocess
- `llm_call.py`: Modelo inválido en `_call_claude` falla silenciosamente sin log
- `memory_sqlite.py`: Deduplicación frágil con `LIKE` en JSON; requiere columna hash UNIQUE
- `clean_trash.py`: Paths hardcodeados `/home/agent/` en vez de `Path.home()`
- `parallel_loop.py`: Doble captura de excepciones confusa en `_run_worker`

**MEDIA**
- `run_logger.py`: Sin bloqueo de archivo en rotación de logs (alta concurrencia)
- `runs_dashboard.py`: Incompatibilidad de schema entre módulos (timestamp vs ISO string)
- `llm_call.py`: Prompt sin separador de roles en `_call_ollama`
- `memory_sqlite.py`: FTS5 vulnerable a caracteres especiales en tokens
- `memory_lib.py`: Fallo silencioso en import/store con `try/except Exception: pass`
- `linker.py`: Comentario `# auto-linked` rompe frontmatter YAML
- `curator.py`: `shutil.move()` sobrescribe archivos sin verificar duplicados

**BAJA**
- `runs_dashboard.py`: No filtra directorios en iteración de agentes
- `llm_call.py`: Acoplamiento frágil a `run_logger._utc_now()`
- `memory_export.py`: Condición de carrera entre chequeo de existencia y conexión
- `orgchart.py`: Código muerto en orden de condiciones
- `nightly_digest.py`: Dependencia frágil del formato exacto de JSON

## Recurrentes
(ninguno)

## Recomendacion
Corregir los 5 hallazgos ALTA en orden de criticidad: memory_sqlite (deduplicación), clean_trash (paths), run_logger (encoding), llm_call (modelo inválido), parallel_loop (excepciones). Para MEDIA, priorizar memory_lib (fallo silencioso) y runs_dashboard (schema inconsistente). Los hallazgos BAJA son cosméticos pero deben documentarse para evitar futuros bugs.

## Hallazgos por cluster (crudo)

### observability
`run_logger.py` · ALTA · `_run_wrapper` captura `Exception` genérica, pero si `subprocess.run` falla por `FileNotFoundError` (comando no existe), el `str(e).encode()` en `err` puede fallar si el mensaje contiene caracteres no-ASCII. Usar `str(e).encode('utf-8', 'replace')` como en el resto del código.

`run_logger.py` · MEDIA · `_maybe_rotate` silencia `OSError` pero no rota si el archivo está en uso; en alta concurrencia (múltiples crons simultáneos) puede perder datos o corromper el log. Considerar bloqueo de archivo (`fcntl.flock`) o logging a syslog.

`runs_dashboard.py` · MEDIA · `parse_cron_runs` asume que `rec.get("ts")` es un timestamp en milisegundos, pero `run_logger.py` guarda `ts` como string ISO 8601 (`_utc_now()`). `dt.datetime.fromtimestamp(ts_ms / 1000, TZ)` fallará con `TypeError` o producirá resultados incorrectos. Incompatibilidad de schema entre módulos del mismo cluster.

`runs_dashboard.py` · BAJA · `parse_vault_runs` itera sobre `agents_dir.iterdir()` sin filtrar solo directorios; si hay archivos sueltos, falla con `AttributeError: 'PosixPath' object has no attribute 'is_dir'` (no, `is_dir()` existe). Pero si hay un archivo que no es directorio, `agent_dir / "runs"` puede no existir y `is_dir()` lo maneja. Sin embargo, si hay un archivo llamado como un agente, se intentará acceder a `agent_dir / "runs"` que fallará silenciosamente. Filtrar con `agent_dir.is_dir()` antes de iterar.

### llm-routing
`llm_call.py` · ALTA · `_call_claude` pasa `--model` con `CLAUDE_MODEL` que por defecto es `"claude-sonnet-4-6"`; el CLI de Claude espera nombres como `"claude-sonnet-4-20250514"`. Si el contenedor gateway rechaza el modelo, la llamada falla silenciosamente con `RuntimeError` genérico, sin loggear que el modelo es inválido. Dificulta depuración en producción.

`llm_call.py` · MEDIA · `_call_ollama` construye el prompt concatenando `system + "\n\n" + user` sin separador de roles; Ollama espera formato chat/template. Si `system` está vacío, el prompt empieza con `"\n\n"`. Puede degradar calidad de respuesta en tareas mecánicas.

`llm_call.py` · BAJA · `_notify_once` usa `run_logger._utc_now()` (módulo externo no mostrado) para timestamp en `FAIL_LOG`. Si `run_logger` no tiene ese atributo o cambia, el log de fallos se rompe sin excepción capturada. Acoplamiento frágil.

### memory
`memory_sqlite.py` · ALTA · El método `store()` usa `metadata LIKE ?` con un patrón JSON para deduplicación. Esto es frágil y puede fallar si el orden de las claves en el JSON serializado cambia o si hay caracteres especiales. Un falso negativo en la deduplicación crea duplicados; un falso positivo (colisión de LIKE) puede omitir un store legítimo. La solución es añadir una columna `hash TEXT UNIQUE` a la tabla `memorias` e indexar por ella.

`memory_sqlite.py` · MEDIA · `_fts_query()` construye una cadena MATCH uniendo tokens con `OR`. Si un token contiene una comilla doble escapada o un carácter de control, la sintaxis FTS5 puede romperse (sqlite3.OperationalError). Aunque el regex `_TOKEN_RE` es restrictivo, `re.findall` en Python 3 puede devolver cadenas vacías o inesperadas para ciertos Unicode. Añadir `token.replace('"', '""')` dentro de las comillas dobles para escapar.

`memory_lib.py` · MEDIA · `_mirror_to_db()` hace un `sys.path.insert` y un `import memory_sqlite` dentro de un bloque `try/except Exception: pass`. Esto silencia cualquier error de importación (ej. módulo faltante, SyntaxError) y también errores de store (ej. DB bloqueada). El fallo silencioso puede ocultar que la DB no se está escribiendo, rompiendo la consistencia prometida (FASE 4). Al menos loggear la excepción con `logging.exception` o `print(..., file=sys.stderr)`.

`memory_export.py` · BAJA · `connect_ro()` abre la DB con `mode=ro` en la URI. Si la DB no existe, `sqlite3.connect` lanza `OperationalError` (no `FileNotFoundError`). El chequeo `if not pathlib.Path(db).exists()` antes de llamar a `connect_ro` es correcto, pero si la DB se borra entre el chequeo y la conexión (condición de carrera, improbable en cron pero posible), la excepción no se captura. Añadir un `try/except sqlite3.OperationalError` alrededor de `connect_ro()` y salir con error.

### vault-maintenance
`clean_trash.py` · ALTA · `TRASH_DIRS` usa paths hardcodeados `/home/agent/` en vez de `Path.home()`. Si el VPS cambia de usuario o se migra, el script no limpiará nada silenciosamente. Usar `Path.home() / "agent-stack" / ...` como el resto del cluster.

`linker.py` · MEDIA · `merge_related()` añade `# auto-linked {date}` al final del frontmatter, no del body. Esto rompe el YAML del frontmatter porque inserta un comentario `#` fuera de un bloque YAML válido, corrompiendo el archivo. Debe ir al final del body o eliminarse.

`curator.py` · MEDIA · `check_lifecycle()` mueve directorios con `shutil.move()` sin verificar que el destino `.archive/` no tenga ya contenido del mismo nombre. Si dos skills de distinta categoría tienen el mismo nombre, el segundo sobrescribe al primero silenciosamente. Usar `shutil.copytree()` + `shutil.rmtree()` con `dirs_exist_ok=False` y log de conflicto.

`orgchart.py` · BAJA · `role_from_subagents()` chequea `if allow:` y `if allow == []:` en orden incorrecto. `allow=[]` es falsy, por lo que `if allow:` lo trata como `False` y nunca llega al `elif allow == []`. No causa bug porque ambos casos devuelven `"worker"`, pero es código muerto y confuso.

### autonomous-loops
`parallel_loop.py` · ALTA · `_run_worker` captura `Exception` genérico, pero `llm_call.llm_call` ya captura errores internos y devuelve `res.ok=False`. Si `llm_call` lanza una excepción no capturada (ej. `KeyboardInterrupt`, `SystemExit`), el `except Exception` en `run_fanout` la atraparía, pero `llm_call` no está diseñado para lanzar — el riesgo es bajo pero la doble captura es confusa y puede ocultar bugs reales en `llm_call`.

`overnight_research.py` · MEDIA · `build_index()` recorre `VAULT.rglob("*.md")` sin límite de profundidad ni exclusión de directorios grandes como `.git` o `node_modules` — ya excluye algunos, pero no `.stfolder`, `__pycache__`, o directorios de 10k+ archivos. En un vault grande (>50k archivos) esto puede saturar memoria o timeout del cron.

`learnings.py` · MEDIA · `collect_runs()` parsea `date_str` con dos formatos, pero si `fm.get("date")` es un entero (ej. `20260529`) o una lista, `isinstance(date_str, str)` falla y cae a `st_mtime`. Esto es correcto, pero `fm.get("date")` puede ser `None` y el código lo maneja — no hay bug, solo fragilidad potencial si el frontmatter tiene `date: 2026-05-29` (string) vs `date: 20260529` (int). No hay fallo real aquí.

`nightly_digest.py` · BAJA · `read_runs()` filtra líneas con `f'"ts":"{date}' in line` — si `ts` contiene la fecha como substring (ej. `"ts":"2026-05-29T02:00:00Z"`), funciona. Pero si el JSON tiene espacios (`"ts": "2026-05-29..."`), falla silenciosamente. No hay bug porque el formato de `runs.jsonl` es consistente (sin espacios), pero es una dependencia frágil del formato exacto.

**Cluster limpio**: sin bugs confirmados, race conditions, ni vulnerabilidades de seguridad en el código mostrado.
