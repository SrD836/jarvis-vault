---
type: chat-message
from: david
role: user
ts: 2026-05-19T12:24:47.291Z
source: dashboard
project: polymarket-veto-loop-bot
related:
  - "[[02-sessions/2026-05-19/121118-configura-el-bot-polymarket-para-operacion-autonoma-247]]"
  - "[[03-decisions/2026-05-19-polymarket-v1-baseline-failure]]"
  - "[[agents/polymarket-bot/memory]]"
  - "[[projects/polymarket-veto-loop-bot/design]]"
  - "[[projects/polymarket-veto-loop-bot/project]]"
# auto-linked 2026-05-19
---


[RETRY tras fix de provider: ahora corres con Claude Max OAuth, no DeepSeek]

El intento anterior creó bot/config.json pero abortó. Sigue desde donde te quedaste — la config ya está en vault/projects/polymarket-veto-loop-bot/bot/config.json con los 12 parámetros correctos. NO la recrees.

OBJETIVO: bot operando 24/7 autónomo en simulation. Lee la config existente y aplica los cambios pendientes:

1. **Refactor Go para leer bot/config.json** (en vez de constantes hardcoded):
   - `bot/scanner/internal/scanner/scanner.go` — sort por endDate asc antes de escribir candidates.jsonl.
   - `bot/brain/internal/brain/brain.go` — gate nuevo P1 entre P0 y memory: veta candidates con días_a_resolución fuera de [config.short_term_window_days[0], config.short_term_window_days[1]] con razón "P1: fuera de ventana sim corto plazo".
   - `bot/brain/internal/rules/rules.go` — V2 lee config.v2_veto_under_hours en vez de 72 hardcoded.
   - `bot/executor/internal/executor/executor.go` — leer config.trade_size_usd, config.max_exposure_usd, config.max_same_category, config.max_new_trades_per_day. Añadir guard: `if config.mode != "simulation" { panic("live trading not authorized") }`.
   - `bot/exit_monitor/internal/monitor/monitor.go` — leer config.stop_loss_usd, config.take_profit_pct.

2. **Helper compartido**: crea `bot/common/config/config.go` con `LoadBotConfig() (*BotConfig, error)` (struct + load from BOT_CONFIG_PATH env var, default `bot/config.json`). Patrón: como loadOrCreatePortfolio. Importar desde los 4 binarios.

3. **Recompilar Go**: `docker run --rm -v <path>:/work -w /work golang:1.22-alpine sh -c "go build -o bin/<comp> ./cmd/<comp>"` para scanner, brain, executor, exit_monitor. Binarios resultantes en bot/<comp>/bin/<comp>.

4. **Crontab del usuario `agent` en host VPS**: añadir dos líneas (no borrar las existentes orgchart/curator/etc.):
```
*/30 * * * * cd /home/agent/agent-stack/vault/projects/polymarket-veto-loop-bot && BOT_CONFIG_PATH=bot/config.json ./bot/scanner/bin/scanner && ./bot/brain/bin/brain && ./bot/executor/bin/executor >> /home/agent/agent-stack/vault/inbox/trading/cron-pipeline.log 2>&1
*/5  * * * * cd /home/agent/agent-stack/vault/projects/polymarket-veto-loop-bot && BOT_CONFIG_PATH=bot/config.json ./bot/exit_monitor/bin/exit_monitor >> /home/agent/agent-stack/vault/inbox/trading/cron-exit_monitor.log 2>&1
```
Edita crontab vía `docker exec` al host o como mejor sepas — eres autónomo en esto.

5. **Vault**: actualiza `vault/agents/polymarket-bot.md` SOLO la sección "## Human notes" añadiendo:
   - Modo: simulation
   - Operación: autónoma 24/7 vía cron del host
   - Ventana: 2-30 días (corto plazo)
   - Config: `bot/config.json`
   - Fecha activación: 2026-05-19
   (el resto del fichero no se toca — orgchart.py lo regenera)

6. **Dashboard Bot page badge** (opcional, si te queda tiempo y tokens): en server.js endpoint /api/bot/pnl añadir campo `operation_mode: "autonomous_24_7"` y `last_cron_run_ts: mtime(cron-pipeline.log)`. En Bot.jsx mostrar badge "Operación: autónoma · último scan hace Xm".

7. **Reporta aquí**: qué hiciste, dónde quedó cada paso, qué falló si algo falló. Sin recapitulaciones largas: una línea por paso con OK/FAIL/SKIP y un detalle si hay.

Si algo se te resiste (p.ej. no puedes editar crontab del host), dilo claramente — no inventes resultados. No me preguntes nada, decide tú según las prioridades de la regla never-API y mode=simulation.

