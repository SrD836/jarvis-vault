import type { ProjectContent } from "../../types";

export default {
  title: "Trabajo · LinkedIn Easy Apply",
  theme: "dark",
  tags: ["typescript", "node", "vue"],
  
  description: "Job search + CV adaptation + LinkedIn auto-apply pipeline with two approval gates. CV adapted per offer via claudemax, dynamic portfolio generated at runtime.",
  components: [],
} as const satisfies ProjectContent;
